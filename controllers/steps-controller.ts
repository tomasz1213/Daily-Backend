import { Request, Response, NextFunction } from 'express';

export const readStepsData = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

export const writeStepsData = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

export const updateStepsData = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

