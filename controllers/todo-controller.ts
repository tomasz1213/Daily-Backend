import { Request, Response, NextFunction } from 'express';

export const readToDoData = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

export const writeToDoData = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

export const updateToDoData = async (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

